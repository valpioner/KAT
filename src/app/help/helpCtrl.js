(function () {
    'use strict';

    angular
        .module('app')
        .controller('helpCtrl', helpCtrl);

    // homeCtrl.$inject = ['$scope'/*, 'TestService'*/];

    function helpCtrl($scope/*, TestService*/) {
        var vm = this;
    }
})();